export const OTP_LENGTH = 6;

/** Seconds the "Resend code" link stays disabled after a code is sent. */
export const RESEND_COOLDOWN_SECONDS = 30;

export const PASSWORD_MIN_LENGTH = 8;
export const USERNAME_MIN_LENGTH = 3;
