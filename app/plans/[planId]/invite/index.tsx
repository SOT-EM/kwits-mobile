import { View, Text } from "react-native";

export default function InvitePeersScreen() {
    return (
        <View style={{ flex: 1, padding: 16 }}>
            <Text style={{ fontSize: 18, fontWeight: "600" }}>Invite Peers</Text>
        </View>
    );
}